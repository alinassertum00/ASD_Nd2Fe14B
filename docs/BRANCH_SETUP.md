# Suggested Git branch

Use the branch name:

`portable-reproducible-workflow`

From a local clone of the repository:

```bash
git checkout -b portable-reproducible-workflow
# copy the contents of this package into the repository root
git add .
git commit -m "Make VAMPIRE workflow portable and reorganize analysis pipeline"
git push -u origin portable-reproducible-workflow
```

After reviewing the branch on GitHub, merge it into `main` through a pull request.
